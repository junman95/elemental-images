# 파일 조회 규칙

이 프로젝트에서 파일을 탐색하거나 코드를 검색할 때는 반드시 **qmd MCP 도구**를 사용한다.

- Glob, Grep 도구 사용 금지
- 파일 목록 조회 → `qmd_ls` 사용
- 코드 내용 검색 → `qmd_search` 또는 `qmd_query` 사용
- 파일 내용 읽기 → `qmd_get` 사용
- 컬렉션: `elemental-images` (TS), `elemental-images-tsx` (TSX)
